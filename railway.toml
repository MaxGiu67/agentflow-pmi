# Railway config — reference only
# Services are configured via Railway dashboard
# This file documents the expected variable wiring

# Service: api
# Dockerfile: Dockerfile.api
# Variables:
#   DATABASE_URL = ${{Postgres.DATABASE_URL}}   (auto-wired by Railway)
#   FRONTEND_URL = ${{frontend.RAILWAY_PUBLIC_DOMAIN_VALUE}}
#   JWT_SECRET_KEY = (generate via Railway)
#   PORT = 8000

# Service: frontend
# Dockerfile: Dockerfile.frontend
# Build args:
#   VITE_API_URL = https://${{api.RAILWAY_PUBLIC_DOMAIN}}
# Variables:
#   PORT = 3000
